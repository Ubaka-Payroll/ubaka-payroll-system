{
  "name": "ubaka",
  "private": true,
  "version": "1.0.0",
  "description": "Ubaka — shared API, desktop attendance app, and management portal",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix portal && npm install",
    "dev:api": "npm run dev --prefix backend",
    "dev:desktop": "npm run dev --prefix frontend",
    "dev:portal": "npm run dev --prefix portal",
    "dev": "concurrently -n api,portal -c blue,green \"npm run dev:api\" \"npm run dev:portal\"",
    "seed:portal": "npm run seed:portal --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
